node_modules
.next
migrations
pnpm-lock.yaml
playwright-report
coverage

# Documentation is not application source — keep Prettier scoped to src/ (mirrors
# eslint.config.mjs, which ignores docs/**). The vault-synced SQUAD design system
# under docs/context/design/ is authored upstream via one-way sync, so reformatting
# any of it here would only diverge from the vault source.
docs/
